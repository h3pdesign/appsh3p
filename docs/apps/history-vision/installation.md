# History Vision Installation

## Requirements

- Xcode
- iOS and macOS runtime targets

## Local Setup

1. Open the project generated from the current project configuration.
2. Build either `HistoryVision-iOS` or `HistoryVision-macOS` scheme.
3. Launch app and validate baseline reading flow.
