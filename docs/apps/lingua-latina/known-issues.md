# Lingua Latina Known Issues

- Lexical database licensing is still tracked as a release decision.
- Some builds may ship without the bundled offline database.
- Optional external AI behavior depends on user configuration and provider availability.
